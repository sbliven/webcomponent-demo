services:
  webcomponent-demo:
    image: caddy:latest
    container_name: caddy-server
    ports:
      - "8050:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./docs/:/srv
      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config: